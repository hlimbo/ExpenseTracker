# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# to init rspec type in:
# bundle expec rspec --init
# this generates a .rspec and spec/spec_helper.rb file

# gem "rails"
gem 'rspec',    '3.6.0'
gem 'coderay',  '1.1.1'
gem 'rack-test', '0.7.0'    
gem 'sinatra',  '2.0.1'
gem 'sequel', '4.48.0'
gem 'sqlite3', '1.3.13'
